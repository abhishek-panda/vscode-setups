{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make run",
            "type": "shell",
            "command": "rm -rf build && mkdir build && make run"
        },
        {
            "label": "Make clean",
            "type": "shell",
            "command": "make clean"
        }
    ]
}